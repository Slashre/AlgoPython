# TP 15 — Les Graphes

## Exercice 1 — Classe Graph (4 pts)
Liste d'adjacence. `add_edge`, `add_directed_edge`.

## Exercice 2 — BFS et DFS (6 pts)
`bfs(src)`, `dfs(src)` (itératif), `dfs_recursive(src)`.

## Exercice 3 — Propriétés (6 pts)
`is_connected()`, `has_cycle()`, `connected_components()`.

## Exercice 4 — Tri topologique (4 pts)
`topological_sort()` via DFS post-order sur un DAG.
