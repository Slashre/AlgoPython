# TP 08 — Les Piles (LIFO)

## Exercice 1 — Stack générique (5 pts)
`Stack` : `push`, `pop`, `peek`, `is_empty`, `__len__`.

## Exercice 2 — Parenthèses équilibrées (4 pts)
`is_balanced(expr)` — O(n).

## Exercice 3 — Évaluateur RPN (5 pts)
`eval_rpn(tokens)` : `["3","4","+","2","*"]` → 14.

## Exercice 4 — MinStack O(1) (3 pts)
`MinStack` : `push`, `pop`, `top`, `get_min()` en O(1).

## Exercice 5 — Tri d'une pile (3 pts)
`sort_stack(s)` : avec une seule pile auxiliaire — O(n²).
