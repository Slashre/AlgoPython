# TP 11 — Arbres Binaires de Recherche (BST)

## Exercice 1 — BST complet (12 pts)
`insert`, `search`, `delete` (3 cas), `height`, `count_nodes`,
`inorder` / `preorder` / `postorder`.

## Exercice 2 — Validation BST (4 pts)
`is_valid_bst(root)` avec bornes min/max — O(n).

## Exercice 3 — Tableau trié → BST équilibré (4 pts)
`sorted_array_to_bst(arr)` — hauteur ≤ ⌈log₂(n+1)⌉.
